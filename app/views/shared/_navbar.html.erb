<nav class="navbar navbar-expand-sm navbar-lewagon sticky-top">
  <div class="container">
    <div class="d-flex align-items-center">
      <%= link_to root_path, class: "navbar-brand" do %>
        <h1><span class="navbar-brand-text">RENTSTA</span></h1>
      <% end %>

      <%= simple_form_for :search, url: listings_path, method: :get, html: { class: "search-form" } do |f| %>
        <div class="input-group">
          <div class="input-group-prepend">
            <%= f.input :query, label: false, input_html: { class: "form-control", placeholder: "Search Stadium" }, wrapper_html: { class: "search-form-control" } %>
          </div>
          <div class="input-group-append">
            <button type="submit" class="btn btn-white">
              <i class="fas fa-search"></i>
            </button>
          </div>
        </div>
      <% end %>
    </div>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav ms-auto">
        <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <%= link_to "#", id: "navbarDropdown", data: { bs_toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false do %>
              <i class="fa-solid fa-user" style="color: #ffffff;"></i>
            <% end %>
            <div class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <%= link_to 'Add Stadium', new_listing_path, class: "dropdown-item"  %>
              <%= link_to 'My listings', my_listings_path, class: "dropdown-item"  %>
              <%= link_to 'Inbox', incoming_booking_requests_path, class: "dropdown-item"  %>
              <%= link_to 'My booking requests', outgoing_booking_requests_path, class: "dropdown-item"  %>
              <%= link_to 'Edit account details', edit_account_path, class: "dropdown-item"  %>
              <%= link_to "Logout", destroy_user_session_path, class: "dropdown-item", data: { turbo_method: :delete } %>
            </div>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to "Add Stadium", new_listing_path, class: "nav-link addlisting-link" %>
            <%= link_to "Sign Up", new_user_registration_path, class: "nav-link signup-link" %>
            <%= link_to "Login", new_user_session_path, class: "nav-link login-link" %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
