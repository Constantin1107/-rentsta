<h2>Booking overview</h2>
<%= link_to 'Home', root_path, class: "btn button" %>
<ul>
  <li><%= @booking.listing.name %></li>
  <li><%= @booking.listing.price %>€</li>
</ul><%= simple_form_for @booking do |f| %>
  <%= f.input :booking_date %>
  <%= f.submit %>
<% end %>
<%= link_to 'Cancel booking', booking_path(@booking.id, active: 'false'), data: { turbo_method: :put }, class: "btn button" %>
